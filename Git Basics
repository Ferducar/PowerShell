GIT BASIC COMMANDS
git init                                to initialize a project
git add <file> or <files>               move files to staging area
git add .                               move all to staging area
git status                              status branch
git commit                              local commit
git commit -m "add commit comment"
git push -u origin <name>	        	push local branch for the first time and link remote and local branch
git push origin <branch>                does not link remote and local branches
git push                                to upload commits (to linked branch)
git push origin --delete <branch>	    delete remote branch
git pull                                to bring all changes
git pull origin my_remote_branch        to bring changes of a specific branc (if they are not linked)
git pull origin <branch> --set-upstream pull, link local and remote branch
git fetch                               to bring status of changes
git clone <url>                         download project
git rm --cached <file>                  to unstage
git log                             
git log --oneline --decorate --all --graph
git restore <file>                      helps to unstage or even discard uncommitted local changes.
git branch                              to create new branches
git branch -u origin/branch             to link remote and local branch
git branch --set-upstream-to=origin/<branch> <local branch>	        link branches later...
git branch -r	                        list remote branches
git branch -a	                        list remote and local branches
git branch -m <new-name>	            rename local head branch
git branch -m <old-name> <new-name>		rename any local branch
git checkout <branch>                   to change branches
git remote show origin                  to show my remote repo


START GIT PROJECT
git config --global user.email "you@example.com"
git config --global user.name "Your Name
git remote set-url origin https://github.com/Ferducar/PowerShell        to modify our origin


PROJECT SETUP
Create a new repository on the command line
git init
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/Ferducar/GitTutorial.git
git push -u origin master

Push an existing repository from the command line
git remote add origin https://github.com/Ferducar/GitTutorial.git
git branch -M master
git push -u origin master

Join a team
git clone  <url> <folder name>
git remote add origin <url>
git push --set-upstream origin master

GIT COMMENTS
Git will ignore empty folders


FEATURE BRANCH GIT AND GITHUB WORKFLOW
Create  a feature branch
upload to git
create pull request
merge feature branch


GITHUB STATES
Working directory > git add > staging area > git commit > repository


Refrences: 
https://www.youtube.com/watch?v=Q1kHG842HoI&ab_channel=SuperSimpleDev
https://www.youtube.com/watch?v=HiXLkL42tMU&ab_channel=Fazt
